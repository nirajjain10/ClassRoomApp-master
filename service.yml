apiVersion: v1
kind: Service
metadata:
  name: sre-demo
  labels:
    app: sre-demo
spec:
  selector:
    app: sre-demo
  type: LoadBalancer
  ports: 
    - protocol: TCP
      port: 8080
      targetPort: 8080